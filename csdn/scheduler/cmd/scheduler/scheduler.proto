syntax = "proto3";

package scheduler;
option go_package="./scheduler";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Scheduler {
  rpc Ping(Request) returns(Response);
}
